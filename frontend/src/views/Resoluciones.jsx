import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  padding: 20px;
`;

const Resoluciones = () => {
  return (
    <Container>
      <h1>Resoluciones</h1>
      {/* Aquí irá el contenido de la vista de resoluciones */}
    </Container>
  );
};

export default Resoluciones; 